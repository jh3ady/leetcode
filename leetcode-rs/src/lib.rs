//! `LeetCode` solutions in Rust.
//!
//! Each problem lives in its own module (`pXXXX_<slug>`) with tests inline.
//! The scaffolder appends new `pub mod` declarations between the two marker
//! lines below. Do not remove them.

#![forbid(unsafe_code)]
#![warn(clippy::pedantic)]
#![allow(clippy::must_use_candidate, clippy::missing_errors_doc)]

// @problems:start
pub mod p0001_two_sum;
// @problems:end
